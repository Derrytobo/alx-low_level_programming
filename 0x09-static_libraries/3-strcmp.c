#include "main.h"
#include <string.h>

/**
 * _strcmp -> compare two strings
 * @s1: First string
 * @s2: Second string
 * Return: Compared result
 */
int _strcmp(char *s1, char *s2)
{
	int x;

	x = strcmp(s1, s2);
	return (x);
}
